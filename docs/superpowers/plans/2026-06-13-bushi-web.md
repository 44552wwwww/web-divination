# 卜术占卜Web应用 · 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 构建一个 Flask Web 应用，用户通过浏览器使用五种占卜工具（小六壬/梅花易数/六爻/奇门遁甲/大六壬），部署到 Render。

**Architecture:** Flask 后端渲染 SPA 页面 + 5个 POST API 路由 → 调纯 Python 计算脚本 → 返回 JSON → 前端 JS 渲染结果卡片。深色主题，响应式布局。

**Tech Stack:** Flask 3.x, gunicorn, vanilla JS (no framework), zero-dependency Python calculators, Render deployment.

**项目目录:** `D:\claudecode牛逼666\web占卜\`

---

## 文件结构

```
web占卜/
├── app.py                       # Flask 主程序 (路由 + API)
├── requirements.txt             # Flask, gunicorn
├── .gitignore
├── README.md
├── static/
│   ├── style.css               # 深色主题全局样式
│   └── app.js                   # 前端交互 (导航切换 + fetch + 渲染)
├── templates/
│   └── index.html              # SPA 单页面 (5个标签 + 输入区 + 结果区)
├── scripts/
│   ├── xiaoliuren.py           # 小六壬计算引擎
│   ├── meihua.py               # 梅花易数计算引擎
│   ├── liuyao.py               # 六爻计算引擎
│   ├── qimen.py                # 奇门遁甲计算引擎
│   └── liuren.py               # 大六壬计算引擎
└── docs/
    └── superpowers/
        ├── specs/
        │   └── 2026-06-13-bushi-web-design.md
        └── plans/
            └── 2026-06-13-bushi-web.md
```

---

### Task 1: 项目初始化

**Files:**
- Create: `D:\claudecode牛逼666\web占卜\requirements.txt`
- Create: `D:\claudecode牛逼666\web占卜\.gitignore`
- Create: `D:\claudecode牛逼666\web占卜\README.md`

- [ ] **Step 1: 创建 requirements.txt**

```
Flask==3.1.0
gunicorn==23.0.0
```

- [ ] **Step 2: 创建 .gitignore**

```
__pycache__/
*.pyc
*.pyo
.env
venv/
```

- [ ] **Step 3: 创建 README.md**

```markdown
# web占卜

五种中华传统占卜工具的 Web 应用。

## 本地运行

pip install -r requirements.txt
python app.py

打开 http://localhost:5000

## 占卜工具

- 小六壬：掐指速算，30秒断吉凶
- 梅花易数：万物起卦，看大方向
- 六爻：铜钱摇卦，标准答案
- 奇门遁甲：时空最优解，择吉布局
- 大六壬：人事显微镜，断纠纷人心

## 部署

Render Web Service: gunicorn app:app
```

- [ ] **Step 4: 安装依赖并验证**

```bash
cd D:\claudecode牛逼666\web占卜
pip install -r requirements.txt
python -c "import flask; print(flask.__version__)"
```

Expected: `3.1.0`

- [ ] **Step 5: Git 初始化**

```bash
cd D:\claudecode牛逼666\web占卜
git init
git add -A
git commit -m "初始化项目结构"
```

---

### Task 2: 小六壬计算引擎

**Files:**
- Create: `D:\claudecode牛逼666\web占卜\scripts\xiaoliuren.py`

**接口:** `compute(month, day, hour_name) -> dict`

六掌诀: 大安(1) 留连(2) 速喜(3) 赤口(4) 小吉(5) 空亡(6)
算法: 月上起月(从大安顺数月数次) → 月上起日(从结果顺数日数次) → 日上起时(从结果顺数时数次)

- [ ] **Step 1: 写计算函数**

```python
#!/usr/bin/env python3
"""小六壬掌诀推算"""

PALMS = {
    1: {"name": "大安", "emoji": "🟢", "desc": "大安事事昌，求谋在东方。失物去不远，宅舍保安康。",
        "advice": "适合出行、求财、见贵人。不要太急，稳扎稳打。", "good": True},
    2: {"name": "留连", "emoji": "🟡", "desc": "留连事难成，求谋日不明。官事宜迟缓，去者未回程。",
        "advice": "事有拖延，不要急。先等等看清楚再行动。", "good": False},
    3: {"name": "速喜", "emoji": "🔴", "desc": "速喜喜来临，求财向南行。失物申午见，行人路上寻。",
        "advice": "好事马上到。往南方走走，丢了东西中午前后找。", "good": True},
    4: {"name": "赤口", "emoji": "⚪", "desc": "赤口主口舌，官非切要防。失物急去寻，行人有惊慌。",
        "advice": "小心口舌是非，别跟人吵架。东西丢了赶紧找。", "good": False},
    5: {"name": "小吉", "emoji": "🔵", "desc": "小吉最吉昌，路上好商量。阳人来报喜，失物在坤方。",
        "advice": "路上遇到好事，有人来帮你。适合和人谈事合作。", "good": True},
    6: {"name": "空亡", "emoji": "⚫", "desc": "空亡事不长，阴人多主张。求财无利益，行人有灾殃。",
        "advice": "这事不太靠谱，别抱太大希望。等等再说。", "good": False},
}

HOUR_MAP = {
    "子": 1, "丑": 2, "寅": 3, "卯": 4, "辰": 5, "巳": 6,
    "午": 7, "未": 8, "申": 9, "酉": 10, "戌": 11, "亥": 12,
}

def compute(month: int, day: int, hour_name: str) -> dict:
    """推算小六壬掌诀"""
    if month < 1 or month > 12 or day < 1 or day > 30:
        return {"success": False, "error": "月(1-12)或日(1-30)不合法"}
    if hour_name not in HOUR_MAP:
        return {"success": False, "error": f"时辰必须是: {'/'.join(HOUR_MAP.keys())}"}

    hour = HOUR_MAP[hour_name]

    # 月上起月: 从大安(1)顺数月数次
    step1 = (1 + (month - 1) - 1) % 6 + 1
    step1_name = PALMS[step1]["name"]

    # 月上起日: 从上一步结果顺数日数次
    step2 = (step1 + (day - 1) - 1) % 6 + 1
    step2_name = PALMS[step2]["name"]

    # 日上起时: 从第二步结果顺数时数次
    step3 = (step2 + (hour - 1) - 1) % 6 + 1

    result = PALMS[step3]
    return {
        "success": True,
        "data": {
            "result": result["name"],
            "emoji": result["emoji"],
            "poem": result["desc"],
            "advice": result["advice"],
            "is_good": result["good"],
            "steps": [
                {"action": f"正月起大安 → 顺数{month}个月", "landing": step1_name},
                {"action": f"{step1_name}起初一 → 顺数{day}天", "landing": step2_name},
                {"action": f"{step2_name}起子时 → 顺数{hour}个时辰", "landing": result["name"]},
            ]
        }
    }


if __name__ == "__main__":
    import json
    print(json.dumps(compute(3, 19, "午"), ensure_ascii=False, indent=2))
```

- [ ] **Step 2: 测试小六壬**

```bash
cd D:\claudecode牛逼666\web占卜\scripts
python xiaoliuren.py
```

Expected: 输出 JSON，含 result/emoji/poem/advice/steps

- [ ] **Step 3: Commit**

```bash
cd D:\claudecode牛逼666\web占卜
git add scripts/xiaoliuren.py
git commit -m "feat: 小六壬计算引擎"
```

---

### Task 3: 梅花易数计算引擎

**Files:**
- Create: `D:\claudecode牛逼666\web占卜\scripts\meihua.py`

**接口:** `compute(method, values) -> dict`

三种起卦方式: number(1-3个数字), time(年月日时), text(任意文字取笔画)

- [ ] **Step 1: 写计算函数**

```python
#!/usr/bin/env python3
"""梅花易数起卦推算"""

GUA = {
    1: {"name": "乾", "symbol": "☰", "wx": "金", "nature": "天"},
    2: {"name": "兑", "symbol": "☱", "wx": "金", "nature": "泽"},
    3: {"name": "离", "symbol": "☲", "wx": "火", "nature": "火"},
    4: {"name": "震", "symbol": "☳", "wx": "木", "nature": "雷"},
    5: {"name": "巽", "symbol": "☴", "wx": "木", "nature": "风"},
    6: {"name": "坎", "symbol": "☵", "wx": "水", "nature": "水"},
    7: {"name": "艮", "symbol": "☶", "wx": "土", "nature": "山"},
    8: {"name": "坤", "symbol": "☷", "wx": "土", "nature": "地"},
}

# 八卦万物类象 (简化版, 用于白话解读)
GUA_MEANING = {
    1: "天，代表刚健、领导、权威、父亲、头",
    2: "泽，代表喜悦、口舌、少女、缺憾",
    3: "火，代表光明、美丽、文书、中女、眼睛",
    4: "雷，代表震动、行动、长子、足、愤怒",
    5: "风，代表进入、传播、长女、不决、股",
    6: "水，代表危险、智慧、中男、耳朵、陷阱",
    7: "山，代表停止、阻碍、少男、手、固执",
    8: "地，代表柔顺、包容、母亲、腹部、大众",
}

def _num_to_gua(n: int) -> int:
    """数字转八卦 (1-8)"""
    return (n - 1) % 8 + 1

def _num_to_yao(n: int) -> int:
    """数字转动爻 (1-6)"""
    return (n - 1) % 6 + 1

def compute(method: str, values: dict) -> dict:
    """梅花易数起卦"""
    if method == "number":
        nums = values.get("numbers", [])
        if not nums or len(nums) < 2:
            return {"success": False, "error": "至少输入2个数字"}
        upper = _num_to_gua(nums[0])
        lower = _num_to_gua(nums[1])
        moving = _num_to_yao(nums[0] if len(nums) < 3 else nums[2])

    elif method == "time":
        y, m, d, h = values.get("year",1), values.get("month",1), values.get("day",1), values.get("hour",0)
        upper = _num_to_gua(y + m + d)
        lower = _num_to_gua(m + d + h)
        moving = _num_to_yao(y + m + d + h)

    elif method == "text":
        text = values.get("text", "")
        if not text:
            return {"success": False, "error": "请输入文字"}
        # 取笔画数(简化: 汉字数×3 + 第一个字笔画)
        strokes = len(text) * 3
        upper = _num_to_gua(strokes)
        lower = _num_to_gua(strokes // 2)
        moving = _num_to_yao(strokes)
    else:
        return {"success": False, "error": f"不支持的方式: {method}"}

    # 动爻: 变换上卦或下卦
    changed_upper = upper
    changed_lower = lower
    if moving <= 3:
        changed_lower = ((changed_lower - 1) ^ 1) % 8 + 1  # 下卦变
    else:
        changed_upper = ((changed_upper - 1) ^ 1) % 8 + 1  # 上卦变

    # 体用: 不动为体，动为用
    if moving <= 3:
        body_gua, use_gua = upper, lower
        body_is = "上卦"
    else:
        body_gua, use_gua = lower, upper
        body_is = "下卦"

    # 体用生克
    wx_body = GUA[body_gua]["wx"]
    wx_use = GUA[use_gua]["wx"]
    order_s = {"木":"火","火":"土","土":"金","金":"水","水":"木"}
    order_k = {"木":"土","土":"水","水":"火","火":"金","金":"木"}

    if order_s.get(wx_use) == wx_body:
        relation = "用生体"
        emoji = "🟢"
        verdict = "大吉！事情主动来找你，不用费力。"
        detail = f"对方（{wx_use}）主动来帮你（{wx_body}），就像水灌溉树木一样自然。事情顺利，等着好消息就行。"
    elif order_k.get(wx_body) == wx_use:
        relation = "体克用"
        emoji = "🟡"
        verdict = "能成，但比较累。"
        detail = f"你（{wx_body}）能克制对方（{wx_use}），像斧头能砍树。但砍完自己也会累。事情能成，但要付出努力。"
    elif order_s.get(wx_body) == wx_use:
        relation = "体生用"
        emoji = "🔴"
        verdict = "消耗自己，可能不划算。"
        detail = f"你（{wx_body}）在滋养对方（{wx_use}），像木头在喂火。不管成不成，你都会损耗很多精力或钱财。三思。"
    elif order_k.get(wx_use) == wx_body:
        relation = "用克体"
        emoji = "⚫"
        verdict = "凶。这事大概率不成，别硬来。"
        detail = f"对方（{wx_use}）克制你（{wx_body}）。硬碰硬会吃亏。建议暂缓或换方向。"
    else:
        relation = "体用比和"
        emoji = "🟢"
        verdict = "大吉！双方和谐，顺风顺水。"
        detail = f"你和对方都是{wx_body}属性，互相理解、配合默契。事情会很顺利。"

    return {
        "success": True,
        "data": {
            "method": method,
            "upper_gua": GUA[upper],
            "lower_gua": GUA[lower],
            "upper_changed": GUA[changed_upper],
            "lower_changed": GUA[changed_lower],
            "moving_yao": moving,
            "body_gua": GUA[body_gua],
            "use_gua": GUA[use_gua],
            "body_is": body_is,
            "relation": relation,
            "emoji": emoji,
            "verdict": verdict,
            "detail": detail,
            "calc_steps": [
                f"上卦: {GUA[upper]['name']}{GUA[upper]['symbol']}（{GUA[upper]['nature']}）",
                f"下卦: {GUA[lower]['name']}{GUA[lower]['symbol']}（{GUA[lower]['nature']}）",
                f"动爻: 第{moving}爻动 → {body_is}变",
                f"体卦({body_is}): {GUA[body_gua]['name']}{GUA[body_gua]['symbol']} {GUA[body_gua]['wx']}",
                f"用卦: {GUA[use_gua]['name']}{GUA[use_gua]['symbol']} {GUA[use_gua]['wx']}",
                f"生克关系: {wx_body} vs {wx_use} → {relation}",
            ]
        }
    }


if __name__ == "__main__":
    import json
    r = compute("number", {"numbers": [37, 5]})
    print(json.dumps(r, ensure_ascii=False, indent=2))
```

- [ ] **Step 2: 测试梅花易数**

```bash
cd D:\claudecode牛逼666\web占卜\scripts
python meihua.py
```

Expected: JSON 含 upper_gua/lower_gua/moving_yao/relation/verdict

- [ ] **Step 3: Commit**

```bash
cd D:\claudecode牛逼666\web占卜
git add scripts/meihua.py
git commit -m "feat: 梅花易数计算引擎"
```

---

### Task 4: 六爻计算引擎

**Files:**
- Create: `D:\claudecode牛逼666\web占卜\scripts\liuyao.py`

**接口:** `compute(lines, question_type) -> dict`

lines: 6次摇卦结果, 每次 "老阴"/"少阴"/"少阳"/"老阳"
question_type: "考试"/"求财"/"感情"/"健康"/"寻人"/"其他"

核心: 排卦→纳支纳干→装六亲→安世应→找用神→断旺衰

- [ ] **Step 1: 写计算函数**

由于六爻代码较长(约300行)，这里给出核心结构和接口定义，完整代码见脚本文件。

```python
#!/usr/bin/env python3
"""六爻/文王卦推算引擎"""

TRIGRAMS = {
    "乾": (1,1,1), "兑": (0,1,1), "离": (1,0,1), "震": (0,0,1),
    "巽": (1,1,0), "坎": (0,1,0), "艮": (1,0,0), "坤": (0,0,0),
}

HEXAGRAM_NAMES = {
    (1,1,1,1,1,1): "乾为天", (1,1,1,0,1,1): "天泽履",  # ... 共64卦
}

# 八卦纳支
NA_ZHI = {
    "乾": ["子","寅","辰","午","申","戌"],
    "坤": ["未","巳","卯","丑","亥","酉"],
    "震": ["子","寅","辰","午","申","戌"],
    "巽": ["丑","亥","酉","未","巳","卯"],
    "坎": ["寅","辰","午","申","戌","子"],
    "离": ["卯","丑","亥","酉","未","巳"],
    "艮": ["辰","午","申","戌","子","寅"],
    "兑": ["巳","卯","丑","亥","酉","未"],
}

# 八卦纳干
NA_GAN = {"乾":"甲壬","坤":"乙癸","震":"庚","巽":"辛","坎":"戊","离":"己","艮":"丙","兑":"丁"}

# 六亲: 以卦宫五行为我, 爻纳支五行为他
ZHI_WX = {"子":"水","丑":"土","寅":"木","卯":"木","辰":"土","巳":"火","午":"火","未":"土","申":"金","酉":"金","戌":"土","亥":"水"}

# 世应位置 (八宫卦)
SHI_YING = {
    "乾": [(5,2),(1,4),(2,5),(3,6),(4,1),(5,2),(6,3),(3,6)],
    "兑": [(5,2),(1,4),(2,5),(3,6),(4,1),(5,2),(6,3),(3,6)],
    # ... 其余六宫
}

SIX_BEASTS = ["青龙","朱雀","勾陈","螣蛇","白虎","玄武"]

YONG_SHEN = {
    "考试": "父母", "求财": "妻财", "感情": "妻财",
    "健康": "官鬼", "寻人": "父母", "其他": "妻财",
}
YONG_SHEN_FEMALE = {
    "感情": "官鬼",
}

Q_TYPE_LABELS = {
    "考试": {"yong": "父母爻", "desc": "代表文书、成绩、录取"},
    "求财": {"yong": "妻财爻", "desc": "代表财富、收益、回报"},
    "感情": {"yong": "妻财爻(男)/官鬼爻(女)", "desc": "代表配偶、感情、缘分"},
    "健康": {"yong": "官鬼爻", "desc": "代表病症、健康问题"},
    "寻人": {"yong": "父母爻", "desc": "代表消息、庇护、下落"},
    "其他": {"yong": "妻财/官鬼", "desc": "根据具体问题判断"},
}

def compute(lines: list, question_type: str, sex: str = "男") -> dict:
    """
    lines: ["少阳","少阴","老阳","少阴","少阳","老阴"]  (从初爻到上爻)
    question_type: "考试"/"求财"/"感情"/"健康"/"寻人"/"其他"
    sex: "男"/"女"
    """
    # 1. 排本卦
    yao_values = []
    for line in lines:
        if line in ("少阳","老阳"): yao_values.append(1)
        else: yao_values.append(0)

    # 2. 找上下卦对应的八卦
    lower_trigram = tuple(yao_values[:3])
    upper_trigram = tuple(yao_values[3:6])

    lower_name = None; upper_name = None
    for name, tri in TRIGRAMS.items():
        if tri == lower_trigram: lower_name = name
        if tri == upper_trigram: upper_name = name

    if not lower_name or not upper_name:
        return {"success": False, "error": "无法识别卦象"}

    # 3. 本卦名
    full_yang = tuple(yao_values)
    hex_name = HEXAGRAM_NAMES.get(full_yang, f"{upper_name}{lower_name}")

    # 4. 变卦
    changed_yao_values = list(yao_values)
    for i, line in enumerate(lines):
        if line in ("老阳","老阴"):
            changed_yao_values[i] = 1 - changed_yao_values[i]

    changed_lower = tuple(changed_yao_values[:3])
    changed_upper = tuple(changed_yao_values[3:6])
    changed_full = tuple(changed_yao_values)
    changed_name = HEXAGRAM_NAMES.get(changed_full, "变卦")

    # 5. 装卦: 纳支纳干六亲世应
    # ... (完整实现在脚本文件中)

    # 6. 用神
    if question_type == "感情" and sex == "女":
        yong_shen_type = "官鬼"
    else:
        yong_shen_type = YONG_SHEN.get(question_type, "妻财")

    # 7. 解读
    yong_info = Q_TYPE_LABELS.get(question_type, Q_TYPE_LABELS["其他"])

    return {
        "success": True,
        "data": {
            "hexagram": hex_name,
            "changed_hexagram": changed_name if changed_name != "变卦" else hex_name,
            "yao_values": yao_values,
            "changed_yao_values": changed_yao_values,
            "moving_lines": [i+1 for i, l in enumerate(lines) if l in ("老阳","老阴")],
            "yong_shen_type": yong_shen_type,
            "yong_shen_desc": yong_info["desc"],
            "question_type": question_type,
            "analysis": f"占问{question_type}，用神取{yong_shen_type}。{'有动爻' if any(l in ('老阳','老阴') for l in lines) else '静卦'}，综合判断。",
        }
    }
```

由于六爻代码非常长（需要64卦名表、八宫卦世应表、纳支纳干六亲装卦等），完整实现将在脚本文件中展开为约350行。这里保持接口清晰，后续实现时补全。剩余4个计算引擎（六爻、奇门、大六壬）和 Flask app + 前端将在完整计划中涵盖。

- [ ] **Step 2-3: 继续完成六爻、奇门、大六壬计算引擎，以及 Flask 主程序和前端**

(为控制单次计划长度，这些任务的详细步骤分开执行。核心架构已在 Task 1-4 中建立，剩余任务遵循相同模式。)

---

### Task 5: Flask 主程序 + API 路由

**Files:**
- Create: `D:\claudecode牛逼666\web占卜\app.py`

API 路由:
- GET `/` → 渲染 index.html
- POST `/api/xiaoliuren` → 调 xiaoliuren.py
- POST `/api/meihua` → 调 meihua.py
- POST `/api/liuyao` → 调 liuyao.py
- POST `/api/qimen` → 调 qimen.py
- POST `/api/liuren` → 调 liuren.py

每个 API 统一返回 `{"success": bool, "data": {...}}` 或 `{"success": false, "error": "..."}`。

### Task 6: 前端页面 + CSS + JS

**Files:**
- Create: `D:\claudecode牛逼666\web占卜\templates\index.html`
- Create: `D:\claudecode牛逼666\web占卜\static\style.css`
- Create: `D:\claudecode牛逼666\web占卜\static\app.js`

### Task 7: 奇门遁甲计算引擎

### Task 8: 大六壬计算引擎

### Task 9: Render 部署配置 + 测试

---

---

### Task 5: Flask 主程序 + API 路由

**Files:**
- Create: `D:\claudecode牛逼666\web占卜\app.py`

- [ ] **Step 1: 写 app.py**

```python
#!/usr/bin/env python3
"""web占卜 · Flask 主程序"""
import sys, os, json
from flask import Flask, render_template, request, jsonify

sys.path.insert(0, os.path.join(os.path.dirname(__file__), 'scripts'))

from xiaoliuren import compute as xiaoliuren_compute
from meihua import compute as meihua_compute
from liuyao import compute as liuyao_compute
# qimen.py 和 liuren.py 完成后取消注释
# from qimen import compute as qimen_compute
# from liuren import compute as liuren_compute

app = Flask(__name__)

@app.route('/')
def index():
    return render_template('index.html')

# ── 小六壬 ──
@app.route('/api/xiaoliuren', methods=['POST'])
def api_xiaoliuren():
    data = request.get_json()
    try:
        result = xiaoliuren_compute(
            int(data.get('month', 1)),
            int(data.get('day', 1)),
            data.get('hour_name', '子')
        )
        return jsonify(result)
    except Exception as e:
        return jsonify({"success": False, "error": str(e)})

# ── 梅花易数 ──
@app.route('/api/meihua', methods=['POST'])
def api_meihua():
    data = request.get_json()
    try:
        result = meihua_compute(
            data.get('method', 'number'),
            data.get('values', {})
        )
        return jsonify(result)
    except Exception as e:
        return jsonify({"success": False, "error": str(e)})

# ── 六爻 ──
@app.route('/api/liuyao', methods=['POST'])
def api_liuyao():
    data = request.get_json()
    try:
        result = liuyao_compute(
            data.get('lines', []),
            data.get('question_type', '其他'),
            data.get('sex', '男')
        )
        return jsonify(result)
    except Exception as e:
        return jsonify({"success": False, "error": str(e)})

# ── 奇门 (占位) ──
@app.route('/api/qimen', methods=['POST'])
def api_qimen():
    return jsonify({"success": False, "error": "奇门遁甲计算引擎尚未完成"})

# ── 六壬 (占位) ──
@app.route('/api/liuren', methods=['POST'])
def api_liuren():
    return jsonify({"success": False, "error": "大六壬计算引擎尚未完成"})


if __name__ == '__main__':
    app.run(debug=True, host='0.0.0.0', port=5000)
```

- [ ] **Step 2: 启动 Flask 测试**

```bash
cd D:\claudecode牛逼666\web占卜
python app.py
```

Expected: `* Running on http://127.0.0.1:5000`

- [ ] **Step 3: 测试小六壬 API**

```bash
curl -X POST http://localhost:5000/api/xiaoliuren -H "Content-Type: application/json" -d "{\"month\":3,\"day\":19,\"hour_name\":\"午\"}"
```

Expected: JSON 含 `{"success": true, "data": {...}}`

- [ ] **Step 4: Commit**

```bash
cd D:\claudecode牛逼666\web占卜
git add app.py
git commit -m "feat: Flask主程序 + 小六壬/梅花/六爻API"
```

---

### Task 6: 前端页面 (HTML + CSS + JS)

**Files:**
- Create: `D:\claudecode牛逼666\web占卜\templates\index.html`
- Create: `D:\claudecode牛逼666\web占卜\static\style.css`
- Create: `D:\claudecode牛逼666\web占卜\static\app.js`

- [ ] **Step 1: 写 style.css**

```css
:root { --bg:#0f0f14; --card:#1a1a24; --c2:#222232; --t:#d4d4dc; --g:#c9a96e; --r:#e85d75; --b:#60a5fa; --gr:#4ade80; --bd:#2a2a3a; }
* { margin:0; padding:0; box-sizing:border-box; }
body { background:var(--bg); color:var(--t); font-family:'Segoe UI','Noto Sans SC','Microsoft YaHei',sans-serif; line-height:1.7; min-height:100vh; }
.nav { display:flex; background:#12121a; border-bottom:1px solid var(--bd); position:sticky; top:0; z-index:100; }
.nav button { flex:1; padding:14px 8px; background:none; border:none; color:#777; cursor:pointer; font-size:0.9em; transition:all .25s; border-bottom:2px solid transparent; font-family:inherit; }
.nav button:hover { color:#aaa; } .nav button.active { color:var(--g); border-bottom-color:var(--g); }
.wrap { max-width:900px; margin:0 auto; padding:30px 20px 60px; }
.hdr { text-align:center; padding:30px 16px; margin-bottom:28px; }
.hdr h1 { font-size:1.8em; background:linear-gradient(135deg,var(--g),var(--b)); -webkit-background-clip:text; -webkit-text-fill-color:transparent; }
.hdr p { color:#888; margin-top:6px; }
/* Form */
.form-card { background:var(--card); border:1px solid var(--bd); border-radius:14px; padding:24px; margin-bottom:20px; }
.form-card label { display:block; color:#888; font-size:0.8em; margin:8px 0 4px; }
.form-card input, .form-card select { width:100%; padding:10px 14px; background:var(--c2); border:1px solid var(--bd); border-radius:8px; color:var(--t); font-size:0.95em; font-family:inherit; }
.form-card .btn { display:block; width:100%; padding:14px; margin-top:16px; background:var(--g); color:#1a1a24; border:none; border-radius:10px; font-size:1.05em; font-weight:700; cursor:pointer; transition:all .25s; font-family:inherit; }
.form-card .btn:hover { filter:brightness(1.1); }
.form-card .row { display:flex; gap:10px; }
.form-card .row > * { flex:1; }
/* Coin */
.coin-area { display:flex; gap:8px; justify-content:center; margin:16px 0; }
.coin { width:60px; height:60px; border-radius:50%; border:2px solid var(--bd); display:flex; align-items:center; justify-content:center; font-size:1.5em; cursor:pointer; transition:all .2s; background:var(--c2); user-select:none; }
.coin:hover { border-color:var(--g); } .coin.flipped { background:#f59e0b; color:#1a1a24; }
.result-card { background:var(--card); border:1px solid var(--bd); border-radius:14px; padding:28px 24px; margin-top:20px; display:none; }
.result-card.show { display:block; }
.result-card .big-verdict { font-size:1.6em; font-weight:700; margin-bottom:8px; }
.result-card .poem { color:#888; font-style:italic; margin:8px 0; line-height:1.8; }
.result-card .steps { background:var(--c2); border-radius:10px; padding:14px; margin:12px 0; font-size:0.85em; color:#aaa; }
.result-card .advice { margin-top:16px; padding:14px; background:rgba(201,169,110,0.06); border-left:3px solid var(--g); border-radius:0 8px 8px 0; line-height:1.8; }
.result-card .advice em { color:#ddd; font-style:normal; font-weight:600; }
.loading { text-align:center; padding:40px; color:#888; display:none; } .loading.show { display:block; }
@media(max-width:600px){ .nav button { font-size:0.75em; padding:12px 4px; } .wrap { padding:16px 10px; } }
```

- [ ] **Step 2: 写 index.html**

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width,initial-scale=1.0">
<title>web占卜 · 五种卜术</title>
<link rel="stylesheet" href="/static/style.css">
</head>
<body>
<div class="nav">
  <button class="active" onclick="switchTool('xiaoliuren')">🖐 小六壬</button>
  <button onclick="switchTool('meihua')">🌸 梅花易数</button>
  <button onclick="switchTool('liuyao')">🪙 六爻</button>
  <button onclick="switchTool('qimen')">🏰 奇门遁甲</button>
  <button onclick="switchTool('liuren')">🌊 大六壬</button>
</div>
<div class="wrap">
  <div class="hdr"><h1>🔮 卜术占卜馆</h1><p>五种传统占卜方式，按需选择</p></div>
  <div id="formArea"></div>
  <div id="loading" class="loading">推算中...</div>
  <div id="resultArea" class="result-card"></div>
</div>
<script src="/static/app.js"></script>
</body>
</html>
```

- [ ] **Step 3: 写 app.js**

```javascript
let currentTool = 'xiaoliuren';

function switchTool(name) {
  currentTool = name;
  document.querySelectorAll('.nav button').forEach((b,i) => b.classList.toggle('active', false));
  event.target.classList.add('active');
  document.getElementById('resultArea').classList.remove('show');
  document.getElementById('resultArea').innerHTML = '';
  renderForm(name);
}

function renderForm(name) {
  const area = document.getElementById('formArea');
  const forms = {
    xiaoliuren: `<div class="form-card">
      <h3 style="color:var(--g)">🖐 小六壬 · 掌诀定位</h3>
      <div class="row">
        <div><label>农历月</label><input id="xlr_m" type="number" value="1" min="1" max="12"></div>
        <div><label>农历日</label><input id="xlr_d" type="number" value="1" min="1" max="30"></div>
        <div><label>时辰</label><select id="xlr_h">${['子','丑','寅','卯','辰','巳','午','未','申','酉','戌','亥'].map(h=>`<option>${h}</option>`).join('')}</select></div>
      </div>
      <button class="btn" onclick="divineXiaoliuren()">掐指一算</button>
    </div>`,
    meihua: `<div class="form-card">
      <h3 style="color:var(--g)">🌸 梅花易数 · 万物起卦</h3>
      <label>起卦方式</label>
      <select id="mh_method" onchange="toggleMHMethod()">
        <option value="number">数字起卦</option><option value="time">时间起卦</option><option value="text">文字起卦</option>
      </select>
      <div id="mh_num"><label>输入2-3个数字(用逗号分隔)</label><input id="mh_nums" placeholder="例如: 37,5"></div>
      <div id="mh_time" style="display:none"><div class="row"><div><label>年</label><input id="mh_y" type="number" value="2026"></div><div><label>月</label><input id="mh_m" type="number" value="6"></div><div><label>日</label><input id="mh_d" type="number" value="13"></div><div><label>时</label><input id="mh_h" type="number" value="12"></div></div></div>
      <div id="mh_text" style="display:none"><label>输入任意文字</label><input id="mh_txt" placeholder="想到什么写什么"></div>
      <button class="btn" onclick="divineMeihua()">起卦</button>
    </div>`,
    liuyao: `<div class="form-card">
      <h3 style="color:var(--g)">🪙 六爻 · 铜钱摇卦</h3>
      <div class="coin-area"><div class="coin" onclick="flipCoin(0)">🪙</div><div class="coin" onclick="flipCoin(1)">🪙</div><div class="coin" onclick="flipCoin(2)">🪙</div></div>
      <p style="text-align:center;color:#888;font-size:0.8em" id="ly_status">摇第1次(共6次)，点硬币翻面后点"记下这一爻"</p>
      <div style="display:flex;gap:8px;margin:8px 0" id="ly_result"></div>
      <button class="btn" id="ly_record" onclick="recordYao()">记下这一爻 (还剩6次)</button>
      <label>问什么事</label><select id="ly_type"><option>考试</option><option>求财</option><option>感情</option><option>健康</option><option>寻人</option><option>其他</option></select>
      <button class="btn" id="ly_submit" onclick="divineLiuyao()" style="display:none">开始占卜</button>
    </div>`,
  };
  area.innerHTML = forms[name] || '<p style="color:#888">即将推出</p>';
  if (name === 'liuyao') initLiuyao();
}

// ── 小六壬 ──
async function divineXiaoliuren() {
  const m = document.getElementById('xlr_m').value;
  const d = document.getElementById('xlr_d').value;
  const h = document.getElementById('xlr_h').value;
  showLoading();
  const res = await fetch('/api/xiaoliuren', {method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({month:parseInt(m),day:parseInt(d),hour_name:h})});
  const data = await res.json();
  hideLoading();
  if (data.success) showXiaoliurenResult(data.data);
  else alert(data.error);
}

function showXiaoliurenResult(d) {
  const emoji = d.is_good ? '🟢' : '⚫';
  let steps = d.steps.map(s => `<p>→ ${s.action}：<b>${s.landing}</b></p>`).join('');
  document.getElementById('resultArea').innerHTML = `
    <div class="big-verdict">${emoji} ${d.result}</div>
    <div class="poem">${d.poem}</div>
    <div class="steps">📐 推算过程：${steps}</div>
    <div class="advice">💡 <em>讲人话就是：</em>${d.advice}</div>`;
  document.getElementById('resultArea').classList.add('show');
}

// ── 梅花 ──
function toggleMHMethod() {
  const m = document.getElementById('mh_method').value;
  document.getElementById('mh_num').style.display = m==='number'?'block':'none';
  document.getElementById('mh_time').style.display = m==='time'?'block':'none';
  document.getElementById('mh_text').style.display = m==='text'?'block':'none';
}

async function divineMeihua() {
  const method = document.getElementById('mh_method').value;
  let values = {};
  if (method==='number') values.numbers = document.getElementById('mh_nums').value.split(',').map(Number).filter(n=>!isNaN(n));
  else if (method==='time') values = {year:document.getElementById('mh_y').value|0,month:document.getElementById('mh_m').value|0,day:document.getElementById('mh_d').value|0,hour:document.getElementById('mh_h').value|0};
  else values.text = document.getElementById('mh_txt').value;
  showLoading();
  const res = await fetch('/api/meihua', {method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({method,values})});
  const data = await res.json();
  hideLoading();
  if (data.success) showMeihuaResult(data.data);
  else alert(data.error);
}

function showMeihuaResult(d) {
  let steps = d.calc_steps.map(s => `<p>→ ${s}</p>`).join('');
  document.getElementById('resultArea').innerHTML = `
    <div class="big-verdict">${d.emoji} ${d.relation} · ${d.verdict}</div>
    <p style="font-size:1.1em;color:#aaa">本卦：${d.upper_gua.symbol}${d.lower_gua.symbol} ${d.upper_gua.name}上${d.lower_gua.name}下</p>
    <div class="steps">📐 推算过程：${steps}</div>
    <div class="advice">💡 <em>讲人话就是：</em>${d.detail}</div>`;
  document.getElementById('resultArea').classList.add('show');
}

// ── 六爻 摇卦 ──
let lyCoins = [0,0,0], lyCount = 0, lyLines = [];

function initLiuyao() { lyCoins=[0,0,0]; lyCount=0; lyLines=[]; updateCoinDisplay(); document.getElementById('ly_record').style.display='block'; document.getElementById('ly_submit').style.display='none'; document.getElementById('ly_result').innerHTML=''; document.getElementById('ly_status').textContent='摇第1次(共6次)'; }

function flipCoin(i) { lyCoins[i]=1-lyCoins[i]; updateCoinDisplay(); }

function updateCoinDisplay() {
  document.querySelectorAll('.coin').forEach((c,i) => {
    c.textContent = lyCoins[i] ? '🪙' : '🪙';
    c.style.background = lyCoins[i] ? '#f59e0b' : 'var(--c2)';
    c.style.color = lyCoins[i] ? '#1a1a24' : '#aaa';
  });
}

function recordYao() {
  const heads = lyCoins.filter(c=>c===1).length;
  let result = heads===3?'老阳':heads===0?'老阴':heads===2?'少阳':'少阴';
  lyLines.push(result); lyCount++;
  document.getElementById('ly_result').innerHTML += `<span style="padding:4px 8px;background:var(--c2);border-radius:6px;font-size:0.8em">${result}</span>`;
  document.getElementById('ly_status').textContent = lyCount>=6 ? '6次完成！点下方开始占卜' : `还剩${6-lyCount}次`;
  if (lyCount>=6) { document.getElementById('ly_record').style.display='none'; document.getElementById('ly_submit').style.display='block'; }
  lyCoins=[0,0,0]; updateCoinDisplay();
}

async function divineLiuyao() {
  if (lyLines.length<6) return alert('先摇完6次');
  const qtype = document.getElementById('ly_type').value;
  showLoading();
  const res = await fetch('/api/liuyao', {method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({lines:lyLines,question_type:qtype})});
  const data = await res.json();
  hideLoading();
  if (data.success) showLiuyaoResult(data.data);
  else alert(data.error);
}

function showLiuyaoResult(d) {
  document.getElementById('resultArea').innerHTML = `
    <div class="big-verdict">🪙 ${d.hexagram}${d.moving_lines.length?' → '+d.changed_hexagram:''}</div>
    <p style="color:#888">用神: ${d.yong_shen_type}(${d.yong_shen_desc}) | 动爻: ${d.moving_lines.join(',')||'静卦'}</p>
    <div class="advice">💡 <em>分析：</em>${d.analysis}</div>`;
  document.getElementById('resultArea').classList.add('show');
}

function showLoading() { document.getElementById('loading').classList.add('show'); document.getElementById('resultArea').classList.remove('show'); }
function hideLoading() { document.getElementById('loading').classList.remove('show'); }

renderForm('xiaoliuren');
```

- [ ] **Step 4: 本地测试**

```bash
cd D:\claudecode牛逼666\web占卜
python app.py
# 浏览器打开 http://localhost:5000
# 测试小六壬、梅花、六爻三个标签
```

- [ ] **Step 5: Commit**

```bash
cd D:\claudecode牛逼666\web占卜
git add templates/ static/ app.py
git commit -m "feat: 前端页面 + CSS + JS + Flask路由"
```

---

### Task 7: 奇门遁甲计算引擎

**Files:**
- Create: `D:\claudecode牛逼666\web占卜\scripts\qimen.py`

**接口:** `compute(year, month, day, hour, q_type) -> dict`

算法: 定局(阴阳遁) → 排地盘(三奇六仪) → 排天盘(九星) → 排人盘(八门) → 排神盘(八神)

简化版: 按时家奇门排盘，输出九宫格数据 + 白话解读

- [ ] **Step 1: 写 qimen.py (约500行)**

完整代码见脚本文件。核心结构:

```python
#!/usr/bin/env python3
"""奇门遁甲时家排盘"""

# 八门: 休生伤杜景死惊开
# 九星: 蓬任冲辅英芮柱心
# 八神: 值符螣蛇太阴六合白虎玄武九地九天 (阳遁顺排)
#       值符螣蛇太阴六合白虎玄武九地九天 (阴遁逆排)

# 阴阳遁定局表 [节气][上中下元]
DUN_TABLE = {
    "冬至": [1,7,4], "小寒": [2,8,5], "大寒": [3,9,6],
    "立春": [8,5,2], "雨水": [9,6,3], "惊蛰": [1,7,4],
    "春分": [3,9,6], "清明": [4,1,7], "谷雨": [5,2,8],
    "立夏": [4,1,7], "小满": [5,2,8], "芒种": [6,3,9],
    "夏至": [9,3,6], "小暑": [8,2,5], "大暑": [7,1,4],
    "立秋": [2,5,8], "处暑": [1,4,7], "白露": [9,3,6],
    "秋分": [7,1,4], "寒露": [6,9,3], "霜降": [5,8,2],
    "立冬": [6,9,3], "小雪": [5,8,2], "大雪": [4,7,1],
}

# 用神映射
YONG_SHEN_QIMEN = {
    "考试": {"yong": "丁奇", "gate": "景门", "desc": "丁奇主文书考试，景门主文章"},
    "开业": {"yong": "开门", "gate": "开门", "desc": "开门主开创、开业、开始"},
    "出行": {"yong": "日干", "gate": "生门", "desc": "日干代表自己，生门主生机"},
    "求职": {"yong": "开门", "gate": "开门", "desc": "开门主工作机会"},
    "谈判": {"yong": "日干", "gate": "惊门", "desc": "日干为自己，惊门主口舌"},
    "求财": {"yong": "戊", "gate": "生门", "desc": "戊为钱财，生门为财源"},
    "搬家": {"yong": "生门", "gate": "生门", "desc": "生门主生机和安居"},
}

def compute(year, month, day, hour, q_type):
    # 1. 确定节气 → 阴阳遁和局数
    # 2. 排地盘: 戊己庚辛壬癸丁丙乙 按局数飞布九宫
    # 3. 时柱旬首 → 值符星 + 值使门
    # 4. 排天盘九星 (值符带星飞布)
    # 5. 排人盘八门 (值使带门飞布)
    # 6. 排神盘八神 (阳顺阴逆)
    # 7. 取用神落宫 → 分析吉凶格
    return {"success": True, "data": { ... }}
```

- [ ] **Step 2: 测试**

```bash
cd D:\claudecode牛逼666\web占卜\scripts
python -c "from qimen import compute; import json; print(json.dumps(compute(2026,6,13,10,'考试'),ensure_ascii=False))"
```

- [ ] **Step 3: 启用 API**

在 `app.py` 中取消 qimen 的 import 注释，替换占位路由。

- [ ] **Step 4: Commit**

---

### Task 8: 大六壬计算引擎

**Files:**
- Create: `D:\claudecode牛逼666\web占卜\scripts\liuren.py`

**接口:** `compute(year, month, day, hour, q_type) -> dict`

算法: 月将加时 → 天盘 → 四课 → 九宗门 → 三传 → 十二天将

- [ ] **Step 1: 写 liuren.py (约800行)**

```python
#!/usr/bin/env python3
"""大六壬课传推算"""

# 十二天将: 贵人螣蛇朱雀六合勾陈青龙天空白虎太常玄武太阴
# 九宗门: 贼克/比用/涉害/遥克/昴星/别责/八专/伏吟/返吟

# 月将表 (中气后换将)
YUE_JIANG = {
    1: "亥", 2: "戌", 3: "酉", 4: "申", 5: "未", 6: "午",
    7: "巳", 8: "辰", 9: "卯", 10: "寅", 11: "丑", 12: "子",
}

# 贵人起法 (昼夜贵)
GUI_REN = {
    "甲":"丑", "戊":"丑", "庚":"丑",
    "乙":"子", "己":"子", "丙":"亥", "丁":"亥",
    "辛":"午", "壬":"巳", "癸":"巳",
}

def compute(year, month, day, hour, q_type):
    # 1. 定月将 → 天地盘
    # 2. 四课: 日干支 + 天地盘取上神
    # 3. 九宗门 → 定三传
    # 4. 十二天将布三传
    # 5. 类神取用 + 白话解读
    return {"success": True, "data": { ... }}
```

- [ ] **Step 2: 测试并启用 API**

同 Task 7。

- [ ] **Step 3: 前端补上奇门和六壬的输入表单、结果渲染**

在 `app.js` 中补全 `renderForm('qimen')` 和 `renderForm('liuren')`。

- [ ] **Step 4: Commit**

---

### Task 9: Render 部署

**Files:**
- Create: `D:\claudecode牛逼666\web占卜\Procfile`

- [ ] **Step 1: 创建 Procfile**

```
web: gunicorn app:app
```

- [ ] **Step 2: 确认 requirements.txt**

```
Flask==3.1.0
gunicorn==23.0.0
```

- [ ] **Step 3: 推送到 GitHub 后，Render 上创建 Web Service**

- New Web Service → 选仓库 → Build Command: `pip install -r requirements.txt` → Start Command: `gunicorn app:app`
- 部署后获得 `https://xxx.onrender.com` 链接

- [ ] **Step 4: Commit + Push**

```bash
cd D:\claudecode牛逼666\web占卜
git add -A && git commit -m "部署配置" && git push
```
